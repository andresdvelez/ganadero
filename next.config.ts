import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Ensure we output standalone for deployment flexibility
  output: undefined,
};

export default nextConfig;
